// type uint256_dynamic_array is uint256;

// library uint256_dynamic_array_lib {
//   function length(uint256_dynamic_array arr) internal pure returns (uint256) {}
//   function readElement(uint256_dynamic_array arr) internal pure returns (uint256) {}
// }

// /*
// If the fn codegen methodology were used, how would one do something like retrieve a pointer to a value?

// Perhaps each read function must return one word and it can be any type?

// REQUIRED FUNCTIONS

// For structs:
// Method to resolve every member to an instance of the type;

// `decode()` returns an instance of the type with all required members



// */

// type IType is uint256;
// // type Pointer is 

// library EncoderABI {
//   function encode_uint256_array(Pointer ptr, uint256_dynamic_array arr) pure returns {

//   }
// }

// interface ITypeCoder {
  
//   // function decode(Pointer ptr) pure returns 
// }