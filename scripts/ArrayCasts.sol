library ArrayCasts {
  function toPointer(bool[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(address[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes1[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes2[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes3[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes4[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes5[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes6[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes7[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes8[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes9[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes10[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes11[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes12[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes13[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes14[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes15[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes16[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes17[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes18[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes19[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes20[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes21[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes22[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes23[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes24[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes25[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes26[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes27[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes28[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes29[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes30[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes31[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(bytes32[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int8[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int16[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int24[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int32[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int40[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int48[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int56[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int64[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int72[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int80[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int88[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int96[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int104[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int112[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int120[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int128[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int136[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int144[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int152[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int160[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int168[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int176[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int184[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int192[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int200[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int208[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int216[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int224[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int232[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int240[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int248[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(int256[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint8[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint16[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint24[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint32[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint40[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint48[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint56[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint64[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint72[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint80[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint88[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint96[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint104[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint112[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint120[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint128[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint136[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint144[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint152[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint160[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint168[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint176[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint184[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint192[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint200[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint208[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint216[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint224[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint232[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint240[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint248[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

  function toPointer(uint256[] memory arr) internal pure returns (MemoryPointer ptr) {
    assembly { ptr := arr }
  }

}